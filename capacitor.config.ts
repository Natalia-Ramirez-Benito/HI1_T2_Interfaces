import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'HI1_T2_Interfaces',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
